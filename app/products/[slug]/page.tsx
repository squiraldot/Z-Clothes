import { notFound } from 'next/navigation'; import Link from 'next/link'; import { getProduct } from '@/lib/blogger'; import { BuyButton } from '@/components/BuyButton';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const p=await getProduct(slug); if(!p) return notFound(); return <main className="product-page"><div className="gallery"><img className="main-product-image" src={p.image} alt={p.title}/><div className="thumb-row">{p.images.map((im,i)=><img key={i} src={im} alt=""/>)}</div></div><div className="product-info"><span className="eyebrow dark">{p.category}</span><h1>{p.title}</h1><div className="rating">★★★★★ <span>4.8 · 120 reviews</span></div><div className="price">₹{p.price.toLocaleString('en-IN')} {p.compareAtPrice&&<del>₹{p.compareAtPrice.toLocaleString('en-IN')}</del>}</div><p className="lead">{p.description}</p><div className="option"><label>Color</label><div className="swatches">{p.colors.map(c=><button key={c} title={c} className="swatch">{c}</button>)}</div></div><div className="option"><label>Size <Link href="/policies#size-guide">Size guide</Link></label><div className="size-row">{p.sizes.map(s=><button key={s}>{s}</button>)}</div></div><BuyButton product={p}/><div className="service-grid"><span>◈<b>Free Shipping</b><small>Across India</small></span><span>◇<b>Secure Payment</b><small>With Dodo</small></span><span>○<b>Easy Returns</b><small>Within 7 days</small></span></div><details open><summary>Product details</summary><div className="rich" dangerouslySetInnerHTML={{__html:p.contentHtml||'<p>Premium materials, relaxed proportions and a modern Z-Clothes silhouette.</p>'}}/></details></div></main>}
